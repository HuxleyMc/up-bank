{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "up-bank": {
      "type": "stdio",
      "command": "node",
      "args": ["./server.mjs"],
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "UP_ACCESS_TOKEN": "${UP_ACCESS_TOKEN}"
      }
    }
  }
}
